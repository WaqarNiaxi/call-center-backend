import { RequestSalePaymentDto } from './request-sale-payment.dt';

describe('RequestSalePaymentDto', () => {
  it('should be defined', () => {
    expect(new RequestSalePaymentDto()).toBeDefined();
  });
});
